---
import BaseLayout from './BaseLayout.astro';
import { TAG_COLORS, DEFAULT_TAG_COLOR } from '../consts';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  tags: string[];
  heroImage?: string;
}

const { title, description, pubDate, updatedDate, tags, heroImage } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<BaseLayout title={title} description={description}>
  <article class="max-w-3xl mx-auto">
    <div class="mb-8">
      <a
        href={base}
        class="inline-flex items-center gap-1 text-sm mb-6 transition-opacity hover:opacity-60"
        style="color: var(--text-secondary);"
      >
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
        </svg>
        목록으로
      </a>
      <h1 class="text-3xl font-bold mb-4" style="color: var(--text-primary);">
        {title}
      </h1>
      <div class="flex flex-wrap items-center gap-3 mb-4">
        <time datetime={pubDate.toISOString()} class="text-sm" style="color: var(--text-secondary);">
          {pubDate.toLocaleDateString('ko-KR', { year: 'numeric', month: 'long', day: 'numeric' })}
        </time>
        {updatedDate && (
          <span class="text-sm" style="color: var(--text-secondary);">
            (수정: {updatedDate.toLocaleDateString('ko-KR', { year: 'numeric', month: 'long', day: 'numeric' })})
          </span>
        )}
      </div>
      <div class="flex flex-wrap gap-1.5">
        {tags.map((tag) => (
          <a
            href={`${base}tags/${tag}/`}
            class="text-xs px-2.5 py-1 rounded-full font-medium transition-opacity hover:opacity-70"
            style={`background-color: ${TAG_COLORS[tag] ?? DEFAULT_TAG_COLOR}20; color: var(--text-primary); border: 1px solid ${TAG_COLORS[tag] ?? DEFAULT_TAG_COLOR}90;`}
          >
            {tag}
          </a>
        ))}
      </div>
    </div>
    {heroImage && (
      <img src={heroImage} alt={title} class="w-full rounded-xl mb-8" />
    )}
    <div class="prose">
      <slot />
    </div>
  </article>
</BaseLayout>
